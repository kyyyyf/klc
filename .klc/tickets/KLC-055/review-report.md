---
ticket: KLC-055
kind: review-report
authority: human
reviewed_by: general-purpose subagent (fresh, no conversation context) + codex exec review --base main
reviewed_at: 2026-07-12
review_depth: full
branch: feature/klc-055-identity
---

# Review report — KLC-055

## Summary

Two independent reviews ran: a fresh `general-purpose` subagent (no
conversation context, per CLAUDE.md) and `codex exec review --base main`.
Codex reported clean ("implements the specified identity fallback chain and
updates intake without introducing any clear regressions"). The fresh review
graded the branch "essentially clean" — both ACs satisfied, the intake rewire
correct, tests thorough — with two LOW notes and no HIGH/MEDIUM. No code fix
was required on this branch.

## Verdict

APPROVED — both ACs satisfied, no HIGH/MEDIUM from either reviewer, 7 tests pass.

## Findings and assessments

### V1 — fresh LOW-1 — stale generated callgraph index still lists `_git_user`

`.klc/index/callgraph/python.json` still references
`core/phases/intake.py::_git_user`, a symbol this branch deletes. Not a
correctness issue (the index is auto-generated by `klc update`), but
inconsistent with the branch.

**Assessment: fix at wave integrate.** The callgraph index is a generated
whole-repo artifact; regenerating it per-branch would conflict at the wave
merge. It will be regenerated once via `klc update` after the wave-1 branches
land, so every deleted/added symbol across 053–056 is refreshed together
rather than four times. Tracked as a wave-integrate step.

### V2 — fresh LOW-2 — new `SystemExit` behaviour (awareness note, no change)

`identity.current()` raises `SystemExit` when no git identity and no `$USER`
is configured, replacing the old `_git_user()` `"unknown"` fallback. The fresh
reviewer confirmed this is coherent, not a latent crash: `intake` is a
user-initiated CLI entrypoint (`if __name__ == "__main__": sys.exit(run(...))`)
with no programmatic `intake.run(` callers outside tests, so `SystemExit`
prints the setup instruction to stderr and exits non-zero — the intended
fail-fast. The only residual note: a bare-container e2e run with git identity
unset AND `$USER` empty would now hard-exit; `$USER` is normally set.

**Assessment: no change.** Intentional behaviour change, correct for a CLI
entrypoint (satisfies AC-2). Documented here for awareness.

## AC coverage

| AC | Status | Evidence |
|----|--------|----------|
| AC-1 | PASS | email→name→`$USER` order; `test_returns_email`, `test_falls_back_to_name`, `test_falls_back_to_user_env` |
| AC-2 | PASS | `SystemExit` w/ `git config --global user.email` message; `test_systemexit_when_all_unset`, `test_systemexit_message` |

## Final state

`python3 -m pytest tests/test_identity.py tests/test_intake_identity.py -q`
→ 7 passed. `_git_user` fully removed from `core/phases/intake.py`; single
call site delegates to `identity.current()`; unused `import subprocess`
dropped. Wave-integrate follow-up: `klc update` to refresh the callgraph index.


---
[!CONFLICT] scope-expansion detected at review:ack-needed
  planned modules: ['core/skills/identity.py', 'core/phases/intake.py']
  actual modules:  ['core/skills', 'intake', 'tests']
  unplanned:       ['core/skills', 'intake', 'tests']
Resolve: update meta.json:affected_modules to include all touched modules, then re-run `klc ack KLC-055`.
