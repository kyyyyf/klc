---
description: Confirm phase work is done (optionally with --pick N or --auto for gate-policy)
argument-hint: <TICKET-ID> [--pick N] [--auto]
allowed-tools: [Bash]
---

Run `klc ack $ARGUMENTS` via Bash and show the result.
