# {{ project_name }}

> Generated by the Codebase Intelligence Framework at {{ generated_at }}.
> Do not edit above this line. Manual notes go inside the `<!-- BEGIN: manual -->`
> block at the bottom of the file.

## Overview

- **Total files**: {{ total_files }}
- **Total lines**: {{ total_lines }}
- **Languages** (by line count):
{% for lang, stats in languages %}
  - `{{ lang }}` — {{ stats.files }} files, {{ stats.lines }} lines
{% endfor %}

## Modules

| Name | Path | Language | Public API | Depends on |
|------|------|----------|------------|------------|
{% for m in modules %}
| `{{ m.name }}` | `{{ m.path }}` | {{ m.language or "-" }} | {{ (m.public_api or []) | length }} | {{ (m.depends_on or []) | join(", ") or "-" }} |
{% endfor %}

{% if cycles %}
### Dependency cycles detected
{% for c in cycles %}
- {{ c | join(" → ") }}
{% endfor %}
{% endif %}

{% if adr_index %}
## Architecture Decision Records

{% for adr in adr_index %}
- [{{ adr.title }}]({{ adr.file }})
{% endfor %}

{% endif %}
## Conventions for Claude

- Prefer editing the affected module's local `CLAUDE.md` before the root.
- Touch only the modules listed in the task spec's `affected_modules`.
- If a change crosses a module boundary, create or update an ADR.
- Never bypass the validator agent — if a spec is incomplete, ask questions.

{% if notes %}
## Notes from the indexer

{% for n in notes[:10] %}
- {{ n }}
{% endfor %}
{% if notes|length > 10 %}
- _… {{ notes|length - 10 }} more; see `framework/index/inventory.json` and `framework/index/modules.json`._
{% endif %}
{% endif %}

<!-- BEGIN: manual -->
<!-- Add project-wide manual notes here. The framework preserves this block. -->
<!-- END: manual -->
