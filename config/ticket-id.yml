# Ticket-id pattern. `klc intake` validates the first positional
# argument against this regex. Override per project by copying this
# file to `.klc/config/ticket-id.yml`.
#
# Defaults to Jira-style keys: 2+ letters, dash, digits.
pattern: "^[A-Z][A-Z0-9]+-\\d+$"
