{# XS/S-track impl plan. Cap ~10 lines, one step. #}
---
ticket: {{ ticket }}
authority: hybrid
---

# Implementation plan (short) — {{ ticket }}

{% for step in steps %}
## step-{{ loop.index }} — {{ step.title }}

**Goal**: {{ step.goal or step.description }}

**RED**: {{ step.red or "write failing test from test-plan.md before code" }}

**GREEN**: {{ step.green or "smallest code change to pass RED" }}

**VERIFY**: `{{ step.verify or (step.tests[0] if step.tests else "see test-plan.md") }}`

**COMMIT**: `{{ ticket }} step-{{ loop.index }}: {{ step.commit_subject or step.title }}`

Affected: {% for f in step.files %}`{{ f }}`{% if not loop.last %}, {% endif %}{% endfor %}

**Depends on**: {{ step.depends_on or "none" }}

{% endfor %}
